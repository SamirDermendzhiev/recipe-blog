export interface Recipe {
  picture?: string;
  name: string;
  instructions: string;
  ingrediants: string;
  author: string;
  date: number;
  authorId: string;
  id: string;
  difficulty: string;
}
